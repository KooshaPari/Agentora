[package]
name = "agent-platform-app"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Binary that glues the core, ports, and adapters together."

[[bin]]
name = "agent-platform"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
agent-platform-core = { path = "../agent-platform-core" }
agent-platform-ports = { path = "../agent-platform-ports" }
agent-platform-adapters = { path = "../agent-platform-adapters" }
clap = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[package]
name = "phenotype-errors"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Error handling for Phenotype"
repository = "https://github.com/KooshaPari/phenoShared"

[dependencies]
phenotype-error-core = { git = "https://github.com/KooshaPari/phenoShared", branch = "main" }
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tokio-test = "0.4"

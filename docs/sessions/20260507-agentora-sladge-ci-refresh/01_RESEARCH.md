# Research

## Local State

- Canonical Agentora was on `ci/pin-trufflehog` at `7b11967`.
- Canonical had unrelated untracked `ARCHITECTURE.md`, so work stayed isolated.
- Existing prepared branch `docs/agentora-sladge-current` was not a
  fast-forward from current head: `git rev-list --left-right --count` reported
  `5 1`.

## Sladge Target Rationale

Agentora is an AI-agent Rust framework with skill systems, tool registries,
two-tier memory, LLM ports, and lifecycle events, so the badge is appropriate
governance disclosure.

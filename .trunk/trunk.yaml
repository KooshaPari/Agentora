# Trunk launcher and plugin source configuration.
# Linter, formatter, security, and CI action policy belongs in root trunk.yaml.
version: 0.1

cli:
  version: 1.22.2

plugins:
  sources:
    - id: trunk
      ref: v1.10.2
      uri: https://github.com/trunk-io/plugins
